%h2 Sign in

%p Sign in using a service below.

- %w[twitter facebook open_id github].each do |prov|
	= link_to omniauth_authorize_path(resource, prov) do
		= image_tag "#{prov}_64.png", :size => "64x64", :alt => prov.provider_titleize
		= prov.provider_titleize